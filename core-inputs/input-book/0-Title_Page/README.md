# 0-Title_Page

The book's title page. Front matter has no manuscript, so `design-brief.md` — the Creation Interview's
settled decisions — is this piece's raw input instead. `future-ideas.md` holds concepts that were once
part of the brief and got superseded, kept rather than deleted in case they're worth revisiting. See
`../README.md` for why front-matter folders differ from story folders, and
`core-outputs/output-book/0-Title_Page/` for what gets produced from this brief (generation code and the
rendered result).

## Status

Design brief settled (see `design-brief.md`). The output interview (requirements: high-res render first,
rest later; `core-outputs/output-book/` as the produced-material home; generation code built as a real
tool, not a throwaway script) is done too — production hasn't started yet.
