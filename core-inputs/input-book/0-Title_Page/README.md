# 0-Title_Page

Reserved slot for the book's title page — see `../README.md` for what a front-matter folder holds
(generation code + source assets, not a manuscript) and `mediawiki-output/Infinity_0` for the design as
interviewed so far.

## Status

Design still being interviewed — nothing generated yet, so nothing else lives here so far. Once the
title page is actually produced, whatever generated it (code, source images, fonts, etc.) goes in this
folder so the result is reproducible from the repo.
